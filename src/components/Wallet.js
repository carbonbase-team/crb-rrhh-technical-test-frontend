/**
 * Wallet Component
*/

import { StyleSheet, View } from 'react-native';

const Wallet = () => {
   /* Reemplazá este componente */
  return (
    <View style={styles.wrapper} />
  );
}

const styles = StyleSheet.create({
  /* Poné tus estilos acá */
  wrapper: {
    backgroundColor: '#fff',
    borderRadius: 20,
    borderStyle: 'dashed',
    borderWidth: 3,
    borderColor: '#ccc',
    padding: 20,
    height: 160,
  },
});

export default Wallet;
